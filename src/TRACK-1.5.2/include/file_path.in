
/* CARE IF EDITING, editing this file (file_path.in) may 
   cause errors when the configure shell script is run.    */

#define Str(a)         # a              /* make a string              */
#define Add(b, c, d)   "" /*Str(b/c/d) string concatenation macro */
#define Addtwo(b, c)   Str(b/c) /*string concatenation macro */

#define  USER       ""    /* /users/nutis/kih                  */

#define  PATHI      getenv("PATHI")
#define  PATHO      getenv("PATHO")
#define  DATPATH    getenv("DATPATH")
#define  SPPATH     getenv("SPPATH")
